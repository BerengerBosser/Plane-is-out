{
  "name": "plane-is-out",
  "version": "5.0.0",
  "description": "Plane is out : aventure coopérative low poly (1 à 4 joueurs) avec serveur de jeu",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node build.mjs",
    "start": "node server/server.js",
    "deploy:build": "npm install && node build.mjs"
  },
  "engines": { "node": ">=18" },
  "dependencies": {
    "esbuild": "^0.28.2",
    "three": "^0.186.0",
    "ws": "^8.21.3"
  }
}
